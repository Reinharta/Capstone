@model IEnumerable<Capstone.Models.DonationBasket>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Organization.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Supporter.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BasketPending)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Received)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Organization.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Supporter.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BasketPending)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Received)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BasketId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BasketId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BasketId })
        </td>
    </tr>
}

</table>
